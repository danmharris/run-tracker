<h1>My Runs</h1>
<div class="runs">
<% @runs.each do |run| %>
  <a class="run" href="/runs/<%= run.uuid %>">
    <h2><%= format('%.2f', run.distance) %>km</h2>
    <span><%= run.timestamp.strftime('%a %d %b') %><span>
  </a>
<% end %>
</div>
